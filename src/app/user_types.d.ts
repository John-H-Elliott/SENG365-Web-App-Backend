type User = {
    "firstName": string,
    "lastName": string,
    "password": string,
    "email": string,
    "auth_token": string
}

type UserId = {
    "userId": number
}

type UserPhoto = {
    "imageFilename": string
}